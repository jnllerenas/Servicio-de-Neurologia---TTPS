{% trans_default_domain 'FOSUserBundle' %}

<div class="clase_reset_pass">
<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-primary" style="margin-top:10px" type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>
</form>
</div>
