   {% if evoluciones is not empty %}
        
    
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Fecha</th>
                        <th>Descripci&oacute;n</th>
                    </tr>
                </thead>
                <tbody>
                    
                    {% for e in evoluciones %}
                        <tr>
                            <td>{{ e.fecha_evolucion|date('d-m-Y') }}</td>
                            <td>{{ e.evolucion }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        
              
    {% else %}
        
        <label> No existen evoluciones para el paciente seleccionado. </label>
        
    {% endif %}
   
     <a href="{{ path('evolucion_homepage_agregar',{}) }}">
               Generar Nueva Evolución
     </a>